<Window x:Class="MVVM.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainView" Height="200" Width="400">
    <StackPanel Margin="15">
        <TextBlock Text="Personal Informations: " />
        <TextBox Text="{Binding NameInput}" Width="50" />
        <TextBox Text="{Binding FirstnameInput}" Width="50" />
        <TextBox Text="{Binding AgeInput}" Width="50" />
        <Button Content="Valider"
                Command = "{Binding ValiderCommand}" />
        <ListBox ItemsSource="{Binding Personnes}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding FirstName}" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Name}" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Age}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>


    </StackPanel>
</Window>